import { Controller } from '@hotwired/stimulus';

export default class InvitationTemplatesController extends Controller<HTMLDivElement> {
  connect() {
    console.log('connect invitation templates')
  }
}