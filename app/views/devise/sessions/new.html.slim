.content
  .container
    / br.xs-30
    / hr.spacer-sm
    .account-wrapper

      .account-body
        = render( \
            @zap_auth_initial_req || @zap_auth_retry ? 'account_body_zapier' : 'account_body_default',
            { path: request.path, resource: resource, zap_auth_initial_req: @zap_auth_initial_req })

      .account-footer