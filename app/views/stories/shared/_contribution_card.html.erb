
<div class="well contribution-card accordion"
    id="<%= type %>-accordion-<%= index %>"
    data-contribution-id="<%= contribution.id %>">
  <div class="row">
    <div class='col-sm-7 vcenter' style='font-size:14px'>
      <%= contribution.contributor.full_name %>
    </div><!--
 --><div class="col-sm-5 text-right vcenter">

      <%= render 'stories/shared/contribution_card_actions',
            { contribution: contribution, type: type, index: index } %>

    </div>
  </div>

  <% if type == 'in-progress' || type == 'next-steps' || type == 'connection' %>
    <div class="row">
      <div class="col-sm-12">
        <%= type == 'next-steps' ? 'Next step:' : 'Status:' %>&nbsp;
        <%= contribution_status(contribution) %>
      </div>
    </div>
  <% end %>

  <div class="row">
    <div class="col-sm-12">

      <div id="collapse-<%= type %>-submission-<%= index %>"
          class="accordion-body collapse">
        <div class="accordion-inner">
          <hr>
          <% if contribution.status == 'contribution' %>
            <div>"<%= contribution.contribution %>"</div>
          <% elsif contribution.status == 'feedback' %>
            <div>"<%= contribution.feedback %>"</div>
          <% end %>
        </div>
      </div>

      <div id="collapse-<%= type %>-info-<%= index %>" class="accordion-body collapse">
        <div class="accordion-inner">
          <hr>

          Role:&nbsp;&nbsp;<%= contribution.role %> <br>
          Email:&nbsp;&nbsp;<%= contribution.contributor.email %> <br>
          <div class="row">

            <div id='edit-<%= type %>-phone-<%= index %>'
                class='col-sm-2 text-center pull-right cc-notes'
                style='padding:0; margin-top:-2px'>
              <i class='glyphicon glyphicon-pencil bip-clickable'></i>
            </div>

            <div id='<%= type %>-phone-<%= index %>' class='col-sm-10 cc-notes'
                style='padding-right:0; margin-bottom:4px'>
                Phone:&nbsp;&nbsp;
              <%= best_in_place contribution.contributor, :phone,
                  id: "#{type}_best_in_place_contribution_#{contribution.id}_phone",
                 url: "/contributions/#{contribution.access_token}",
          html_attrs: { style:'width:100%' },
           activator: "#edit-#{type}-phone-#{index}",
        place_holder: "<i>add phone ...</i>" %>
            </div> <br style='line-height:10px'>

          </div>
          Referred by:&nbsp;&nbsp;<%= contribution.referrer_id.present? ? contribution.referrer.full_name : "n/a" %> <br>
          Notes: <br>
          <div class="row">

            <div id='edit-<%= type %>-notes-<%= index %>'
                class='col-sm-2 text-center pull-right cc-notes'
                style='padding:0; margin-top:-2px'>
              <i class='glyphicon glyphicon-pencil bip-clickable'></i>
            </div>

            <div id='<%= type %>-notes-<%= index %>' class='col-sm-10 cc-notes'
                style='padding-right:0; margin-bottom:4px'>
              <%= best_in_place contribution, :notes, as: :textarea,
                  id: "#{type}_best_in_place_contribution_#{contribution.id}_notes",
                 url: "/contributions/#{contribution.access_token}",
          html_attrs: { style:'width:100%' },
           activator: "#edit-#{type}-notes-#{index}",
        place_holder: "<i>add notes ...</i>" %>
            </div> <br style='line-height:10px'>

          </div>
          <% if type == 'connection' %>
            LinkedIn profile: <br>
            <div class="row">

              <div id='edit-<%= type %>-linkedin-<%= index %>'
                  class='col-sm-2 text-center pull-right cc-notes'
                  style='padding:0; margin-top:-2px'>
                <span class='glyphicon glyphicon-pencil bip-clickable'></span>
              </div>

              <div id='<%= type %>-linkedin-<%= index %>' class='col-sm-10 cc-notes'
                  style='padding-right:0'>
                <%= best_in_place contribution.contributor, :linkedin_url,
                          url: "/contributions/#{contribution.access_token}",
                   html_attrs: { style:'width:100%' },
                    activator: "#edit-#{type}-linkedin-#{index}",
                 place_holder: "<i>add url ...</i>" %>
              </div>

            </div>
            <% if contribution.linkedin %>
              <br style='line-height:10px'>
              <div class='row text-center'>

                <script type="IN/MemberProfile"
                       data-id="<%= contribution.contributor.linkedin_url %>"
                   data-format="inline" data-related="false" data-width='340'></script>

              </div>
            <% end %>
          <% end %>

        </div>
      </div>
    </div>
  </div>
</div>