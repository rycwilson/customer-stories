
<ul id="stories-gallery" class="grid list-unstyled text-center">

  <% cache(cache_key, skip_digest: true) do %>

    <% stories.each do |story| %>

      <%= render 'stories/shared/card', { company: company, story: story } %>

    <% end %>

  <% end %>

</ul>