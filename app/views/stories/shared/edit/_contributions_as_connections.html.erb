
<div class="tab-pane fade" id="connections">
  <div id='contribution-connections'>

    <% @story.contributions_as_connections.each_with_index do |contribution, index| %>

      <%= render 'stories/shared/edit/contribution_card',
            { contribution: contribution, contributor: contribution.contributor,
              type: "connection", index: index } %>

    <% end %>

  </div>
</div>