(function () {

  var retargetAds = <%= raw @retarget_ads.to_json %>,
      nativeAdsPreviewHtml = <%= raw @native_ads_preview_html.to_json %>,
      imageAdsPreviewHtml = <%= raw @image_ads_preview_html.to_json %>;
      // adType = "<%= @type %>";  // native, image, text

  // console.log(retargetAds);

  // if (adType === 'native') {

    $('#native-ads-panel').html(nativeAdsPreviewHtml);

  // } else if (adType == 'image') {

    $('#image-ads-panel').html(imageAdsPreviewHtml);

  // } else if (adType == 'text') {

    // $('#text-ads-panel').html(previewHtml);

  // }

}());