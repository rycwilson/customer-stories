
(function () {

  var referrerTypes = <%= raw @referrer_types.to_json %>,
      uniqueVisitors = <%= raw @unique_visitors.to_json %>

  initGoogleCharts(false, { referrerTypes: referrerTypes,
                            uniqueVisitors: uniqueVisitors });

}());