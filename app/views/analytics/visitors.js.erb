(function () {

  var visitors = <%= raw @visitors.to_json %>,
      table = $('#measure-visitors-table').DataTable();

  console.log(visitors)

  table.clear();
  table.rows.add(visitors);
  table.draw();

}());