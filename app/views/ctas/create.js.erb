
(function () {

  var cta = <%= raw @cta.to_json({ methods: [:type] }) %>,
      template = _.template($('#cta-template').html());

debugger;

  $('#ctas-list').append(template({ cta: cta }))
  $('#ctas-form')[0].reset();

}());