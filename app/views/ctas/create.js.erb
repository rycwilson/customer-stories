
(function () {

  var newAction = <%= raw @new_action.to_json({ methods: [:type] }) %>,
      template = _.template($('#cta-template').html());

  $('#ctas-list').append(template({ action: newAction }))
  $('#ctas-form')[0].reset();

}());