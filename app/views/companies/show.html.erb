
<% content_for :head do %>
  <!-- challenge: get the cache preview to be responsive to tab selection
       workaround: no preview-cache  -->
       <!-- changed to no-cache for consistency with edit -->
  <meta name="turbolinks-cache-control" content="no-cache">
<% end %>

<!-- include templates for both companies#show and stories#edit -->
<% content_for :underscore_templates do %>
  <%= render 'companies/crowdsource//successes/successes_table_header_template' %>
  <%= render 'companies/crowdsource/successes/success_details_template' %>
  <%= render 'companies/crowdsource/successes/success_actions_dropdown_template' %>
  <%= render 'companies/crowdsource/contributors/contributors_table_header_template' %>
  <%= render 'companies/crowdsource/contributors/contributor_details_template' %>
  <%= render 'companies/crowdsource/contributors/contributor_actions_dropdown_template' %>
  <%= render 'companies/crowdsource/contributors/csp_linkedin_widget_template' %>
  <%= render 'companies/promote/adwords_status_dropdown_template' %>
  <%= render 'companies/promote/promoted_story_actions_dropdown_template' %>
  <%= render 'stories/index/index_template', { company: @company } %>
  <%= render 'stories/edit/content/results_templates' %>
  <%= render 'stories/edit/content/video_templates' %>
<% end %>

<!-- include modals for both companies#show and stories#edit -->
<%= render 'companies/crowdsource/successes/new_success_modal', { company: @company } %>
<%= render 'companies/crowdsource/contributors/new_contributor_modal', { company: @company } %>
<%= render 'companies/crowdsource/contributors/contribution_request_modal', { company: @company} %>
<%= render 'companies/crowdsource/contribution_content_modal' %>
<%= render 'stories/shared/new_story_modal', { company: @company } %>
<%= render 'stories/show/video_modal' %>

<div id='wrapper'>

  <%= render 'shared/company_navbar',
        { company: @company, workflow_stage: @workflow_stage } %>

  <div class='tab-content'>

    <%= render 'companies/crowdsource/crowdsource',
          { company: @company, workflow_stage: @workflow_stage,
            tab: @prospect_tab } %>

    <%= render 'companies/curate/curate', { company: @company, story: @story,
            workflow_stage: @workflow_stage, curate_view: @curate_view,
            curate_story_tab: @curate_story_tab } %>

    <%= render 'companies/promote/promote',
          { company: @company, workflow_stage: @workflow_stage, } %>

    <%= render 'companies/measure/measure',
                { company: @company, workflow_stage: @workflow_stage,
                  recent_activity: @recent_activity,
                  story_views_30_day_count: @story_views_30_day_count } %>

  </div>  <!-- tab content -->

</div>  <!-- wrapper -->
