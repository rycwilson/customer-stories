(function () {

  var flashMesg = <%= raw @flash_mesg.to_json %>,
      flashStatus = <%= raw @flash_status.to_json %>

  $('header.navbar').attr('style', '<%= admin_navbar_style %>');

  flashDisplay(flashMesg, flashStatus);

})();