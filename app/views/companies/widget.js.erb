
(function () {

  // delete the cookie so curator can view the widget behavior
  Cookies.remove(CSP.company.subdomain + '-hide-widget')

  $('.widget-form-confirm').fadeToggle('fast', 'swing', function () {
    var _$this = $(this)
    setTimeout(function () {
      _$this.fadeToggle('fast')
    }, 3000);
  })

}());