
<%= form_for(template, url: url,
      html: { method: method, remote: true,
              id: 'crowdsourcing-template-form' }) do |template_form| %>

  <div class="row">

    <div class="col-lg-8">

      <div class="form-group">
        <%= template_form.label(:name, "Name") %>
        <%= template_form.text_field(:name, class: 'form-control') %>
      </div>

      <div class="form-group request-subject">
        <%= template_form.label(:request_subject, "Email Request&nbsp;&#8211;&nbsp;Subject".html_safe) %>
        <div class="input-group">
          <%= template_form.text_field(:request_subject, class: 'form-control') %>
          <div class="input-group-btn">
            <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Insert&nbsp;&nbsp;<span class="caret"></span></button>
            <ul class="dropdown-menu data-placeholders">
              <li data-placeholder='[customer_name]'>
                <a href="javascript:;">Customer name</a>
              </li>
              <li data-placeholder='[contributor_first_name]'>
                <a href="javascript:;">Contributor first name</a>
              </li>
              <li data-placeholder='[contributor_full_name]'>
                <a href="javascript:;">Contributor full name</a>
              </li>
            </ul>
          </div>
        </div>
      </div>

      <div class="form-group request-body">
        <%= template_form.label(:request_body, "Email Request&nbsp;&#8211;&nbsp;Body".html_safe) %>
        <%= template_form.text_area(:request_body, id: 'email-request-body-editor',
                data: { provider: 'summernote' }) %>
      </div>

    </div>

    <div class="form-group contributor-questions col-lg-4">
      <%= render('companies/settings/contributor_questions',
            { company: company, template: template, template_form: template_form }) %>
    </div>

  </div>

  <hr>

  <div class="row">
    <div class="col-sm-12 text-right">
      <div class="form-group">

        <button type="reset" class="btn btn-default">Cancel</button>
        <button type="submit" class="btn btn-success text-center"
                style='<%= method == 'post' ? 'width: 135px' : 'width: 114px' %>'>
          <span><%= method == 'post' ? 'Create template' : 'Save changes' %></span>
          <i class='fa fa-spinner fa-pulse' style='display:none'></i>
          <i class='fa fa-check' style='display:none'></i>
        </button>

      </div>
    </div>
  </div>

<% end %>