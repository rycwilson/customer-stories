<!-- promote -->
<div id='promote-panel'
     class="<%= workflow_tab == 'promote' ? 'active tab-pane content' : 'tab-pane content' %>" role='tabpanel'>

  <div class='container'>
    <div class='row'>
      <div class='portlet portlet-default'>

        <%= form_tag '/ads', method: :get, remote: true, id: 'get-ads-form' do %>

          <button type='submit' class='btn btn-primary'>
            Get Ads
          </button>

        <% end %>

      </div>  <!-- portlet -->
    </div>  <!-- row -->
  </div>  <!-- container -->
</div>  <!-- promote