# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

/.ruby-version

# editor config
/.vscode

# mac system files
**/.DS_Store

# log and temp files
/log
!/log/.keep
/tmp
/yarn-error.log

# dependencies
/node_modules
/vendor/assets/*
!/vendor/assets/javascripts
!/vendor/assets/stylesheets
!/vendor/assets/*.zip

# figaro-managed credentials
# no push to either github or heroku
# to set env variables in heroku (when new vars are added),
# run this in local environment (app_name is csp-staging or floating-spire-2927):
#   figaro heroku:set -e production -a <app_name>
/config/application.yml

# heroku local config
/.env

# compiled assets are served from aws cloudfront/s3, so only need to track the manifest
/public/assets/**
!/public/assets/.sprockets-manifest-*
/public/dev-assets/**
!/public/dev-assets/.sprockets-manifest-*

/app/assets/builds/*
!/app/assets/builds/.keep
/app/typescript/emit/*